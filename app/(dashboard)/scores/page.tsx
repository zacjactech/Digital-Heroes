// Scores page — route: /scores
// Stitch reference: /stitch/score_entry_interface/ and /stitch/mobile_score_entry/
export default function ScoresPage() {
  return <div className="p-8"><p>Scores — implement from stitch</p></div>
}
